"use client";

import ReglaListado from "../regla-listado";

export default function RecontactoPage() {
  return (
    <ReglaListado
      categoria="MARKETING"
      basePath="/dashboard/notificaciones/recontacto"
      descripcion="Reglas de recontacto por WhatsApp — mensajes de marketing para traer de vuelta a un cliente."
    />
  );
}
